using System;
class Program
{
    static void Main()
    {
        string[] names = { "faisal;", "jimale", "salah", "said", "Mohamed" };

        foreach (string name in names)
        {
            Console.WriteLine(name);
        }
    }
}
